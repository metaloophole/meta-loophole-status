- name: Run Script (Final Munna Edition)
  run: |
    python3 <<EOF
import requests

url = "https://raw.githubusercontent.com/metaloophole/meta-loophole-status/main/status.json"
res = requests.get(url)

if res.status_code == 200:
    data = res.json()
    if data.get("loophole_active") == True:
        msg = "⚠️ Loophole ACTIVE! Go change PFP now 😈"
        bot_token = "7588713286:AAHF_S7ZxD62YI3JDoPtdnvroiOWbA-M2CQ"
        chat_id = "-1002814683347"
        telegram_url = f"https://api.telegram.org/bot{bot_token}/sendMessage"
        payload = {"chat_id": chat_id, "text": msg}
        requests.post(telegram_url, data=payload)
EOF
